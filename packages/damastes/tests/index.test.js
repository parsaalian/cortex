/* eslint-disable */
// @flow
import sizing from '..';

describe('sizing', () => {
  it('should calculate sizes correctly', () => {
    /* expect(sizing('t')).toEqual({ width: 4, height: 18 });
    expect(sizing('e')).toEqual({ width: 7, height: 18 });
    expect(sizing('s')).toEqual({ width: 6, height: 18 });
    expect(sizing('t')).toEqual({ width: 4, height: 18 }); */
  });
});

import initialState from '~/redux/stores/editor/initialState';
// import { CHANGE_STYLE } from '../constants/actions/editor';

export default function stylingReducer(state = initialState.format, action) {
  return state;
}

import React, { Component } from 'react';
import './Char.css';

export default class Char extends Component {
  constructor(props) {
    super(props);
    this.state = { cursor: true };
  }

  render() {
    return(<div></div>);
  }
}
